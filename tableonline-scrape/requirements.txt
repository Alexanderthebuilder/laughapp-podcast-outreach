# HTTP + parsing
httpx[http2]==0.28.1
selectolax==0.3.27
ijson==3.3.0
openpyxl==3.1.5          # contact sheet export
python-dotenv==1.0.1

# Rendering (Phase 1a-bis crosscheck, Phase 2 detail, Phase 4 Tier 3).
# Imported lazily — phases that do not render run without it installed.
playwright==1.49.1

# Tests
pytest==8.3.4

# Trade register extracts (src/phase5_virre.py parse). Imported lazily.
pypdf==6.18.0

# Name judging (src/ai_review_names.py). Imported lazily — the crawl phases
# run without it, but --import-less judging needs it.
anthropic==1.4.0
